Pins that a heredoc body nested inside a process-substitution span (<( ... )) is consumed by the same heredoc line-consumption logic used at top level -- the guard must not mistake the heredoc's quote characters (here, a triple-single-quoted Python string) for an unterminated shell quote just because the heredoc happens to live inside a nested <( ) span rather than at the top level.
