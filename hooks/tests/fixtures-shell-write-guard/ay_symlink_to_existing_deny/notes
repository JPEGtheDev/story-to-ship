Pins that a redirect to a symlink is resolved to its real path before the protected-target check: link-to-target.txt is a tracked symlink pointing at target.txt, so writing through it must deny and the stdout must name target.txt (the resolved real path), not the symlink's own name -- proving realpath resolution runs before the git checks rather than the guard treating the symlink's literal path as the candidate.
