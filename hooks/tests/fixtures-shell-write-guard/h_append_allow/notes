Pins that a >> append redirect to an existing tracked file is allowed -- append never truncates or destroys existing content, so it does not trigger the overwrite guard even though the target is protected under the deny rules. The input carries agent_id because this case pins the subagent path; the main-thread counterpart is bj_main_append_redirect_deny.
