Schema disclosure: the Workflow tool_input shape used in this fixture ({"script": <inline text>}) is SCHEMA-DOC-DERIVED, not captured from a live session -- no live-session Workflow PreToolUse payload was available to copy from.

Contract pinned: exactly one agent( call, on a single line, with no model: key anywhere in the script and no WORKFLOW-MODEL-INHERIT-OK marker. The guard must deny (permissionDecision "deny") and the reason must name the offending call -- asserted here via the distinctive prompt-text fragment "investigate the flaky test failure in ci", which only appears in the un-pinned call's arguments. Per the bootstrap-gate-pre.sh precedent, a PreToolUse hook communicates deny via JSON on stdout while still exiting 0 (process exit code is not the deny signal) -- expect_exit is left at its default (0) for this reason.
